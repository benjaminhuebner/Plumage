nonisolated enum GitHubOAuthConfig {
    // Client ID of the registered GitHub OAuth App with Device Flow enabled.
    // Empty keeps "Sign in with GitHub" hidden — pasting a PAT still works.
    static let clientID = "Ov23lif625tEbBNZhiBh"
}
